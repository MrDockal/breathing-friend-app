import { createIconSetFromFontello } from 'react-native-vector-icons';
const fontelloConfig = require('../../fonts/bf_icons.json');

export const CustomIcon = createIconSetFromFontello(fontelloConfig);